<?php
// PHP 5.3.0++
spl_autoload_register(function ($class) {
    switch ($class) {
{{#each contexts}}
{{#classmapeach this}}
      case '{{to-ns namespace '/' "\^" 'firstupper'}}\\{{ key }}DAO':
        include __DIR__ . '{{#if path}}/{{path}}{{/if}}/{{file}}.php';
        break;
{{/classmapeach}}
{{/each}}
    }
});
