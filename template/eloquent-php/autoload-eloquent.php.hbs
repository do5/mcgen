<?php
// PHP 5.3.0++
spl_autoload_register(function ($class) {
    $classMap = array(
{{#each contexts}}
{{#classmapeach this}}
      '{{replace namespace '/' "\^" 'firstupper'}}\\{{ key }}DAO' => __DIR__ . '/{{path}}/{{file}}-eloquent.php',
{{/classmapeach}}
{{/each}}
    );
    if (isset($classMap[$class])) {
        include $classMap[$class];
    }
});



